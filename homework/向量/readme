###
注意！！！使用文件I/O
###
homework1包含以下内容：在主函数中测试无序向量的置乱、查找（实部和虚部均相同）、插入、删除和唯一化的操作。
生成一个有10个元素的复数向量a+bi，且a，b均小于3，以此保证有重复项。
其余效果可直接查看输出文件。
homework2：以复数的模为基准进行排序（模相同的情况下，以实部为基准），比较顺序、乱序、逆序的情况下，起泡排序和归并排序的效率（clock()函数记时）
homework3：在顺序的复数向量中，实现区间查找算法，查找出模介于[m1,m2) 的所有元素，按序存于一个子向量中作为返回值。
部分函数原型：
search(m1,m2,lo,hi)

